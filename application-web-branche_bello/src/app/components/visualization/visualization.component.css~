
h2 {
  color: #2c3e50;
}
ul {
  list-style: none;
  padding: 0;
}
li {
  background: #ecf0f1;
  margin: 5px 0;
  padding: 10px;
  border-radius: 4px;
}


/* visualization.component.css */
.card-container {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
  padding: 20px;
}

.card {
  background-color: #f9f9f9;
  border: 1px solid #ddd;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
}

.card-header {
  background-color: #4CAF50;
  color: white;
  padding: 15px;
  text-align: center;
}

.card-body {
  padding: 20px;
}

.weather-info {
  margin-top: 15px;
}

.card-footer {
  text-align: center;
  padding: 10px;
  background-color: #f1f1f1;
}

button {
  background-color: #4CAF50;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

button:hover {
  background-color: #45a049;
}

.iconsoleil {
  display: inline-block;
  width: 24px; /* Taille de l'icône */
  height: 24px;
  background-image: url('../../../assets/img/soleil.png'); /* Chemin vers l'icône */
  background-size: contain; /* Ajuste l'icône à l'intérieur */
  background-repeat: no-repeat; /* Pas de répétition */
  background-position: center; /* Centre l'icône */
}

.iconnuageux {
  display: inline-block;
  width: 24px; /* Taille de l'icône */
  height: 24px;
  background-image: url('src/assets/img/nuageux.png'); /* Chemin vers l'icône */
  background-size: contain; /* Ajuste l'icône à l'intérieur */
  background-repeat: no-repeat; /* Pas de répétition */
  background-position: center; /* Centre l'icône */
}

.iconneige {
  display: inline-block;
  width: 24px; /* Taille de l'icône */
  height: 24px;
  background-image: url('data:image/png;base64, iVBORw0KGgoAAAANSUhEUgAAABgAAAAYCAYAAADgdz34AAABaElEQVRIS2OQmQ0AAADGwT8tYXOAAAAAElFTkSuQmCC'); /* Chemin vers l'icône */
  background-size: contain; /* Ajuste l'icône à l'intérieur */
  background-repeat: no-repeat; /* Pas de répétition */
  background-position: center; /* Centre l'icône */
}
